import { Routes } from '@angular/router';

// Routes are intentionally empty - this is a single-page game interface.
// Add routes here if the application expands to multiple views (e.g., settings, inventory screen).
export const routes: Routes = [];
